# M4 트윈·임베드 카메라·연출 — 2026-09-13

기준: v2.5 최종 패키지, M3 최종 리뷰, 단계별 테스트 계획.
[입력 SHA256](evidence/M4_sources.log). 사용자 요청을 문서보다 우선한다.

## 한 것

### M3 후속 수정

첫 커밋은 필수 4건과 회귀·증거를 분리한다.

1. `hello`에 BT에서 읽은 `observe_drone`, `mission_marker_ids`, `marker_id_offset`을 추가했다. 프론트의 판독 드론 추론과 고정 마커 범위를 제거했다. BT 값이 없으면 null이며 프론트가 임의 범위를 만들지 않는다.
2. 외부 이름 감사가 `static/**/*.js`의 문자열·숫자 리터럴을 검사한다. 주석과 동봉 vendor는 제외하고 파일·리터럴 수를 출력한다. 이스케이프 문자열·템플릿 내부 표현식과 숫자 범위 회귀를 포함했다.
3. 연결 대기를 내러티브 바 위 우측으로 이동했다.
4. 헤더를 `min-height`, 단계 알약 패딩을 고정 8px/16px로 바꾸고 헤더 자손 경계를 검사한다. 4K 헤더는 **71px**이며 자식이 모두 포함된다. 이전 64px 고정에서는 run-summary가 위·아래로 약 5px씩 넘쳤다.

## 증거

- Docker Ubuntu 22.04/ROS Humble/Python 3.10: **254 passed**(기존 224 + 후속 30). [전체](evidence/M4_followup_unit_green.log), [후속 RED](evidence/M4_followup_backend_red.log), [최종 GREEN](evidence/M4_followup_backend_final_green.log).
- Node 표시 모델/기존 WS **14 passed**. [RED](evidence/M4_followup_model_red.log), [GREEN](evidence/M4_followup_model_green.log).
- 실제 Chromium 3840×2160에서 헤더 내부 넘침 [RED](evidence/M4_followup_header_red.log) → [GREEN](evidence/M4_followup_header_green.log).
- [작성 코드 감사](evidence/M4_followup_constraints_green.log): Python 8, 작성 JS 5, vendor JS 2 제외. 측정 시 병렬 준비 중인 M4 twin 두 파일도 포함한 작업 트리 수치이며, 첫 수정 커밋 자체의 작성 JS는 3개다.

## 스펙과 다르게 한 것 (있으면)

- 후속 수정은 최신 리뷰 지시 그대로다. 4K 헤더 64px 상한은 v2.5 리뷰의 `min-height` 지시로 대체됐다.

## 열린 질문 / 사용자 결정 필요

- 후속 수정에서 계약 변경이 필요한 항목은 없다.

## 다음 마일스톤에서 알아야 할 것

- M4 본 구현과 시각·30분 검증은 이어서 이 보고서에 추가한다. 이후 M5→M7을 연속 진행한다.
